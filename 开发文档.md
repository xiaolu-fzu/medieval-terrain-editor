# 地形编辑器（中世纪小镇 · Tiny Swords 风格）开发文档

> 项目目录：`E:\dev\project\project1\游戏开发\地形编辑器`
> 核心文件：`index.html`（单文件编辑器，含 HTML/CSS/JS）
> 模板素材：`assets/`（小剑 Tiny Swords Free Pack 提取）
> 本地服务：`python -m http.server 8131 --bind 127.0.0.1`（访问 `http://127.0.0.1:8131/`）
> GitHub：`xiaolu-fzu/medieval-terrain-editor`（master）

---

## 1. 项目概述

一个**像素瓦片地形编辑器**，用于为"中世纪小镇"风格的像素游戏制作地形。核心是**多层地形体系**：第 0 层（基础水/草地）+ 第 1~3 层（高处地面），并支持 **悬崖自动生成、楼梯、场景装饰（景观）、角色放置、水泡/阴影特效、导入导出**。

设计原则（贯穿全程）：
- **图层独立**：地形（layers）、楼梯（stairs）、景观（landscape）、角色（units）是**相互独立的叠加层**，互不删除、可遮挡。
- **规则驱动**：地形拼接/悬崖段位/草唇顶等全部由**邻居掩码规则**决定（不手动贴瓦片）。
- **z 轴层级**：水 → 水泡 → 地形 → 楼梯 → 景观 → 角色（从下到上的绘制顺序）。

---

## 2. 技术栈与运行

| 项 | 值 |
|--|--|
| 语言 | 原生 HTML / CSS / JavaScript（无框架、无构建） |
| 画布 | `<canvas>`，`TS=64` 像素/格 |
| 图层数 | `NLAYER=4`（0/1/2/3） |
| 瓦片集 | `color1~color5`（576×384，9列×6行，列4为分隔空列）；高地区域=列5~8（x偏移+320） |
| 动画 | `requestAnimationFrame` + `animTime`（时间驱动帧格 `Math.floor(animTime*fps)%frames`） |
| 数据持久化 | **JSON 导出/导入** 文件 |

---

## 3. 数据模型（核心）

```js
layers[l][r][c]   // 地形：0=空/水, 1=草地/草面, 2=悬崖；0层：0=水, 1=草地
stairs[]          // 楼梯（额外贴图）：{ l, r, c, dir }  dir: 3=左楼梯, 4=右楼梯（锚点=上格, 下格自动）
landscape[]       // 景观：{ l, x, y, kind, id, scale }  x/y=像素中心, scale=大小倍率
units[]           // 角色：{ l, x, y, role, scale }
layerVis[4]       // 图层显隐
curLayer          // 当前绘制层
tab               // 'terrain' | 'landscape' | 'units'（三个选项卡）
brush             // 地形画笔：water/grass/cliff/stairL/stairR/erase
landBrush         // 景观画笔：place/erase/glove
unitBrushTool     // 角色画笔：place/erase/glove
landscapeMode     // 'geom'(几何,吸附格) | 'free'(自由,任意坐标)
landScale/unitScaleVal  // 景观/角色缩放（放置时）
foamScale/shadowScale   // 水泡/阴影大小（初始 3 格）
waterFoamOn/shadowOn    // 水泡/阴影开关（默认开）
```

### 地形值语义
| 层 | 0 | 1 | 2 | 3 |
|--|--|--|--|--|
| 值 0 | 水 | 空 | 空 | 空 |
| 值 1 | 草地 | 草面(高地) | 草面 | 草面 |
| 值 2 | — | 悬崖 | 悬崖 | 悬崖 |

---

## 4. 图层体系与绘制顺序

每个图层绘制顺序（z 轴，从下到上）：

```
1. 第0层：铺水(water_bg) → 水泡特效(0层) → 0层草地/悬崖
2. 第1~3层：水泡特效(仅1层) → 阴影特效(落草地悬崖) → 草面 → 悬崖
3. 楼梯（额外贴图）
4. 景观（叠加贴图）
5. 角色（待机动画）
6. 网格线 / 状态栏
```

---

## 5. 功能详解

### 5.1 第0层（Flat Ground / 水·草地）
- 画笔：**水 / 草地 / 橡皮擦**（0 层没有楼梯）。
- 草地岸边用 **16 掩码自动拼接**：`AUTOTILE[mask]`（mask=NESW，bit=1 表示相邻同是草地），覆盖 16 种岸边形态。
- `mask0(r,c)`：0 层掩码（同层邻居）。

### 5.2 第1~3层（Elevated Ground / 高处地面）
- 画笔：**草地 / 悬崖 / 左楼梯 / 右楼梯 / 橡皮擦**。
- **草面**：`maskHill(l,r,c)` 掩码（只看同层草地=值1，悬崖/空不算邻居），取高地区瓦片（`AUTOTILE[mask].sx+320` 高地区偏移）。
- **闭合端/边界**：草面掩码决定 1~16 各形态（竖条 13/14/15、孤立 16 等）。
- **草唇顶**：草地"上面有草 + 下面露出"才用纯草唇行（y=128,7~9/15）。
- **悬崖（值2）** 段位 = **基础段位（左右邻是否悬崖）+ 草唇顶修正**：
  - 基础：孤立→窄(20/24)、左空→左端(17/21)、右空→右端(19/23)、中间→中(18/22)。
  - 草唇顶修正（左右邻是否草唇顶=草地或楼梯）：17/21 左草唇顶→18/22；19/23 右草唇顶→18/22；20/24 左→19/23、右→17/21、都→18/22；18/22 不变。
  - **落水/落地**：悬崖格下方 0 层=水 → 落水(21~24,y=320)；=草地 → 落地(17~20,y=256)。
- **楼梯**（额外贴图 `stairs`）：同一列两格（上=草坡25/28、下=岩底29/32），方向由 `dir` 定（3=左坡 x=0，4=右坡 x=192）。
  - 楼梯**上格**参与吃草唇边框；**下格不参与**（只与悬崖交互——悬崖 `isLipTop` 用 `isStairAnyAt` 上下格都算）。
  - 楼梯颜色跟随当前层（`LAYERDEF[l].color`）。

### 5.3 景观选项卡（场景装饰）
- 独立 `landscape` 数组，叠加渲染，与地形互不删除、可遮挡。
- **分类与素材**（Tiny Swords）：
  - 灌木 bush（4种，8帧动画）
  - 树 tree（**4种** Tree1~4；Tree1/2 高256、Tree3/4 高192，8帧动画）
  - 树桩 stump（4种，静止）
  - 岩石 rock（4种，静止）
  - 云 cloud（单帧大图，静止）
  - 建筑 building（Blue 阵营 8 种：城堡/民房1-3/修道院/兵营/箭楼/塔，静止，自定义尺寸）
  - 金矿 gold（金堆+金矿1-6，静止）
  - 资源 resource（木头/肉/工具1-4，静止）
  - 鸭子 duck（3帧动画）
  - 水中岩石 wrock（16帧动画）
  - 绵羊 sheep（6帧待机动画）
- **工具**：放置 / 橡皮擦（只删景观，可按住拖动连续删）/ 手套（就近选中、拖动移动、滚轮缩放）。
- **摆放模式**：几何（吸附格子中心）/ 自由（任意坐标；**单击放置，长按拖动=移动最近对象**）。
- **大小可调**：`scale`（0.3~4，等比例缩放）。

### 5.4 角色选项卡（单位）
- 独立 `units` 数组。
- **5 种角色**（Blue 阵营，待机帧动画，参考 castle-2d-test 的角色动画模式）：
  - 弓箭手 archer（6帧/192）、枪兵 lancer（12帧/320）、僧侣 monk（6帧/192）、步兵 pawn（8帧/192）、武士 warrior（8帧/192）。
- 待机动画：`Math.floor(animTime*fps) % frames`（时间驱动）。
- 放置/擦除/手套 同景观；底部中心锚定、叠加最上层。

### 5.5 特效（水泡 / 阴影）
- **水泡**（`water_foam.png`，3072×192=16帧×192）：
  - 判定：**0层草/悬崖邻水**（四向任一为水）→ 水泡；**1层悬崖落水**（下方0层是水）→ 水泡；其他层不要。
  - 望天层（z：水→水泡→地形），画在落水地形格中心，地形盖中间、边缘露水面。
  - 帧错开动画（每格不同起帧），大小 `foamScale`（初始3格）。
- **阴影**（`shadow.png`，192×192 **单帧**，静止）：
  - 判定：**落草地悬崖**（1-3层悬崖，其下方0层是草地=值1）→ 阴影。
  - 画在悬崖格中心（望天层），大小 `shadowScale`（初始3格）。
  - 开关「阴影:开/关」。

### 5.6 图层面板
- 画布大小（行/列 + 应用大小）。
- 图层列表（显隐勾选、当前层高亮）。
- 三个选项卡（地形/景观/角色）切换，互不干扰。

### 5.7 导入导出（JSON）
- 数据结构：
```json
{ "version":1, "cols":40, "rows":24, "curLayer":1, "layerVis":[...],
  "layers":[[[...]]], "stairs":[...], "landscape":[...], "units":[...] }
```
- 导出：`terrain-map.json`（Blob 下载）。
- 导入：读取 json → `applyData(d)` 还原（地形/楼梯/景观/角色/大小/图层）。
- **示例1**：`fetch('terrain-map.json')` 加载当前目录示例地图（必须 http 访问；file:// 下浏览器禁止 fetch，会提示改用 http）。

---

## 6. 素材清单（assets/）

### tiles/（瓦片集 + 特效）
- `color1~color5.png`（576×384 瓦片集；color1=第0层，color2~4=第1~3层）
- `water_bg.png`（水底）、`water_foam.png`（16帧水泡）、`shadow.png`（单帧阴影）

### landscape/（场景素材）
- bush1~4、tree1~4、stump1~4、rock1~4、cloud1、rubber_duck、wrock1/3/4、sheep_idle
- gold_resource、gold_stone1~6、wood_resource、meat_resource、tool1~4
- buildings/（blue）：castle、house1~3、monastery、barracks、archery、tower

### units/（角色）
- archer、lancer、monk、pawn、warrior（待机 6~12 帧）

---

## 7. 关键函数速查

| 函数 | 作用 |
|--|--|
| `mask0(r,c)` | 0层 水/草 掩码 |
| `maskHill(l,r,c)` | 高地草面掩码（只看草地值1） |
| `AUTOTILE[mask]` | 16 掩码 → 草面瓦片坐标（0层坐标；高地 +320） |
| `drawFoam(l)` | 水泡特效（0层邻水 + 1层落水悬崖） |
| `drawShadow(l)` | 阴影特效（落草地悬崖） |
| `isStairAt(l,r,c)` | 楼梯上格（草面吃边框用） |
| `isStairAnyAt(l,r,c)` | 楼梯上/下格（悬崖交互用） |
| `draw()` | 主渲染（水→水泡→地形→楼梯→景观→角色→网格） |
| `toggle(r,c)` | 地形绘制 |
| `placeAt(px,py)` | 景观/角色 放置/擦除/手套 |
| `applyData(d)` | 导入还原（示例1/导入共用） |
| `startLoop()` | RAF 动画循环 |

---

## 8. 已知约定 / 注意事项

1. **必须用 http 访问**（`http://127.0.0.1:8131/`），`file://` 下 fetch/本地资源受限。
2. 瓦片集列 4（x=256）是**分隔空列**，不作为有效瓦片。
3. 高地区（列5~8）与 0 层（列0~3）几何相同、位置不同，绘制时 **+320 偏移**。
4. `shadow.png` 是**单帧**（勿当作16帧动画）。
5. 树 Tree1/2（高256）与 Tree3/4（高192）尺寸不同，渲染用 **per-image 尺寸**。
6. 楼梯/景观/角色为**额外贴图**，不影响 `layers` 地形值（互不删除）。
7. 橡皮擦/手套**只作用于当前选项卡的对象**，不影响地形。
8. 角色素材来自 `Units/Blue Units`（弓箭手/枪兵/僧侣/步兵/武士），仅待机动作。
